import { SearchsubcategoryPipe } from './searchsubcategory.pipe';

describe('SearchsubcategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsubcategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
