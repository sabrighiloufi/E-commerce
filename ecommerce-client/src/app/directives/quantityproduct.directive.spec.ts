import { QuantityproductDirective } from './quantityproduct.directive';

describe('QuantityproductDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantityproductDirective();
    expect(directive).toBeTruthy();
  });
});
